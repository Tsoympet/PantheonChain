# Test Structure

- `tests/unit/`: isolated unit tests.
- `tests/integration/`: multi-process/system-level tests.
- `tests/fixtures/`: static test data and config fixtures.

- `tests/integration/adversarial-multinode.sh`: simulates multi-node equivocation + churn and asserts detectors alert.
