# Relayers Source Namespace

Runtime relayer binaries are currently built from repository root `relayers/` for compatibility with existing build wiring.
This directory is reserved for source migration to the unified `src/` tree.
